# 米游社运营助手 · 迭代记录

> 配套阅读：`README.md`（功能与技术细节）、`DEMO_SCRIPT.md`（3 分钟演示动线）。
> 本文档回答另一个问题：**这个项目是怎么一步步长成现在这样的**——
> 每个版本为什么做、关键决策怎么定、怎么验收。文中日期与数字均对应真实 git 提交与测试基线。

## 演进一览

| 阶段 | 日期 | 主题 | 回答的问题 | 质量基线 |
|------|------|------|-----------|---------|
| v2.x | 初始版本 | 功能跑通 | —— | 无测试、无索引 |
| v3.1 | 2026-09-19 | 原地重构：数据正确性 + 设计系统 + 信息架构 | 数据可信吗？看着像成品吗？ | 测试 0 → 61 项 |
| v4.0 | 2026-09-19 | 「星空琉璃」视觉换装 | 第一眼像游戏公司做的吗？ | 61 项全绿 + 12 张截图验收 |
| v4.1 | 2026-09-19 | 数据能力升级：留存分析 + 异动归因 | 懂不懂数据运营的「数」？ | 70 项全绿 + 13 张截图验收 |
| v4.1 交付 | 2026-09-19~20 | 打包出 exe + 交付链路 | 面试官能不能双击就用？ | 端到端启动验证 |
| v4.2 | 2026-09-20 | 早报智能体（感知-决策-产出）+ 演示数据对齐 4.5 | 会不会用 AI 做运营？判断交给谁？ | 74 项全绿 + 14 张截图验收 |
| v4.3 | 2026-09-20 | 工程加固：异动确认三维度 + 早报闭环 + 质量基建 | 这些结论可信吗？代码能长期维护吗？ | 81 项全绿 + ruff 零告警 + CI 三层 |

---

## v2.x · 起点：先审计，再动刀

动第一行代码之前先做了一次全量代码审计，产出 P0/P1/P2 分级问题清单——
后来 v3.1、v4.1 的修复清单全部来自这份账：

- **数据正确性 P0 × 4**：自动风险标记取错列（风险名存成数字）；智能报告前半段
  结果被后半段整段覆盖（四段式是死代码）；AI 调用无超时；「快捷保存」只弹提示不落库。
- **工程欠账**：全库零索引、每次查询新建连接、无 WAL，README 却把「无索引也稳定」
  当卖点；大量裸 `except: pass`（保存失败却提示成功）；版本号多处不一致；
  图表宽度硬编码 700/680，换分辨率就崩。
- **质量保障**：零测试——改任何一行都不知道改坏了什么。

## v3.1 · 原地重构（2026-09-19）

**为什么原地重构而不是另起炉灶**：业务流程（录入、查重、报表）是能用的，
问题集中在数据层质量与表现层；保留 Mixin 架构可用部分，旧视图整体移入 `_legacy/` 留档可回退。

**三条线并行**：

1. **数据正确性**：重写 `db.py`（13 普通索引 + 4 唯一索引、WAL、`safe_int/safe_float`
   入口净化），修 8 类数据缺陷，其中最典型的一个是静默失效——
   「星铁 3.6 的上一版本」被跨游戏全局排序算成「原神 5.2」，JOIN 出 0 行、
   自动风险标记悄悄失灵；新增 `previous_version()` 按游戏过滤后取相邻版本。
   其余包括：`CREATE TABLE` 里的 `UNIQUE` 对老库不生效导致 `ON CONFLICT` 报错
   （拆独立唯一索引）、LIKE 通配符未转义（搜索 `%` 全表命中）、
   重复录入直接 INSERT 让 DAU 翻倍（改为确认覆盖）等。
2. **设计系统**：`theme.py` 五级明度阶梯（背景分层代替同色平铺，层级自然浮现）、
   `icons.py` 33 个 Canvas 手绘矢量图标（33 图标 × 3 尺寸 × 3 颜色 = 297 组合验证）、
   `components.py` 组件库、`charts.py` 自适应图表。
3. **信息架构**：五导航收口（一个能力只有一个入口），排期/预算/风险统一进版本详情抽屉，
   全局搜索提到 Ctrl+K 命令面板。

**质量体系从 0 到 1**：`smoke_test.py` 30 项 + `flow_test.py` 31 项 = 61 项。
其中「布局不变量」做了**故障注入验证**——故意去掉 Row 色条的 height，断言立刻红
（抓到 `[250, 250, 250, 250]` 幽灵高度），证明测试真能拦住回归，不是摆设。

**本轮最大发现**：空 `CTkFrame` 默认保留 250px 高度且不会收缩，
是「卡片里一大片空白」的唯一根因——抽 `vbox/Row` 组件显式 `height=1`，全项目统一修 113 处。

**验收**：61 项全绿；截图逐张肉眼比对后才算过。

---

## v4.0 · 「星空琉璃」视觉换装（2026-09-19）

**先出方案再动代码**：用真实布局渲染了三套完整配色
（星空琉璃 / 霓虹赛博 / 星穹金曜）对比，确认方向后才落地，避免「换一半发现不对」。

**核心工程动作：PRIMARY / DANGER 语义拆分**。
v3.1 的 `PRIMARY=#FF4D6A` 同时承担「品牌红」和「警戒红」两个语义，
直接换主色会让高风险/逾期/超支/错误提示**全部跟着变紫**。所以换装第一步是拆：

| 色 | 值 | 承担语义 |
|----|----|----------|
| `PRIMARY` | `#7B6BFF` 星轨紫 | 按钮 / 导航 / 进度 / 折线 / 选中态 |
| `DANGER` | `#FF4655` 警戒红 | 高风险 / 逾期 / 预算超支 / 错误 toast |

grep 审计 5 个视图共 **45 处**危险语义用法，按行号从后往前批量替换（避免行号漂移），
品牌用法保持不动，兼容层 `MHY_RED = DANGER` 兜底旧引用。

**诚实的技术边界**：Tk 不支持渐变、半透明、毛玻璃、发光——
交付的是游戏感的**骨架**（深色冷调阶梯、大圆角、胶囊导航、语义分色），不是贴图皮肤。
这条边界明确写进 README，不含糊带过。

**运营语义约定**：StatCard 环比「涨绿跌红」（业务指标语义），
与行情「涨红跌绿」刻意区分——两类「涨跌」在同屏出现时不能让用户读错。

**验收**：61 项全绿 + 12 张截图逐页验收（重点核对风险红/超支红/逾期红在紫底上可辨）。

---

## v4.1 · 数据能力升级（2026-09-19）

**动机**：v3.1 解决「对」、v4.0 解决「好看」，这一版回答数据运营岗位最关心的问题——
**工具懂不懂「数」**。四条线并行：

### ① 留存分析（分析页第 4 个 Tab）

- 数据模型：`daily_metrics` 增加 `new_users`、`retention_1/7/30`，ALTER 幂等迁移老库。
- **三个口径决定**（面试可讲）：
  1. **BI 汇总口径，不做用户级 Cohort**——运营工具通常拿不到明细日志，硬造 Cohort 是假精度；
  2. **0 = 未统计，不是 0%**——否则老库迁移后曲线砸到 0 再跳回 45，像数据事故；
     `valid_retention` 过滤 + 折线断线处理；
  3. **环比用 pp 不用 %**——留存率已是比例，45→40 写成 -11% 是口径放大，-5pp 才是事实。
- 可视化：三卡（均值 + pp 环比 + 45% 健康线红强调）+ 次留/7留/30留三线曲线
  + 7留/次留粘性系数 + 交叉归因文案（留存跌×互动率跌 → 内容吸引力整体回落；
  留存跌×互动率稳 → 新增渠道质量波动）。
- 全链路：录入校验（上限 100%）、CSV 导入（含「次留」等中文别名）与导出、明细列。

### ② 实时异动归因（总览页风险卡自动浮出）

- `db.anomaly_report()`：**最近有数据的一天** vs **前 7 日均值**——
  锚定数据日防止「今天还没录数」的假异动；7 日均值对冲周内节律
  （周三比周日跌 20%，多半是节律不是异动）。
- 阈值常量表 `ANOMALY_RULES`：DAU ±10% / 新增 ±12% / 互动率 ±0.5pp——
  百分比与 pp 量纲分开，规则不同。
- 多游戏库 DAU 异动自动拆分到游戏维度（「拆分：星铁（拖累 18%）」），
  **只统计当日有数据的游戏**——未录数游戏的 cur=0 会被误判成 -100% 拖累（实测踩到后修掉）。
- 风险卡分「人工记录」+「实时检测」两层；**上涨不进风险卡**——暴涨是机会不是风险，
  风险卡只收需要出手的事。

### ③ 测试方法论修正

异动类测试全部改用**确定性造数**（插入临时行 + finally DELETE）：
seed 里加一个随机调用会改变整个随机消耗序列，让所有依赖演示数据绝对值的测试连锁失效
（实际踩到：新增字段后，潜伏的 StatCard 解包 bug 被「随机」触发）。

### ④ 顺带修掉两个真 bug

| 问题 | 触发方式 | 修复 |
|------|----------|------|
| StatCard 的 delta 直接二元组解包，纯文字形态（"11/19 已完成"）ValueError | v3.1 起潜伏，随机序列一变就炸 | 按类型分派：tuple / 字符串 / None |
| `_insert_metric` 下标取值，KeyError 被 except 吞掉，覆盖写入**假成功** | flow_test 部分字段用例 | 改 `rec.get(k, 0)` 缺省按 0 落库 |

**验收**：测试 61 → **70 项**（新增留存数据层、上限校验、异动确定性用例、多游戏拆分定位）；
13 张截图全部通过遮挡/黑帧校验；`DEMO_SCRIPT.md` 3 分钟逐字稿 + 追问应答表就绪。

---

## v4.1 · 工程交付（2026-09-19 ~ 09-20）

- **git 仓库**：4 个真实 commit，演进过程可回放——
  `5b43ed3` v4.1 初始提交 → `dffa3f7` exe 出库 → `01f2847` frozen 路径修复 → `c377bd9` 打包指南。
- **打包前必修的 bug**：onefile 模式 `__file__` 指向临时解包目录，`DB_PATH` 跟着走 =
  **用户数据重启即丢**。修复：frozen 下数据库落到 exe 旁 `data/`；
  验收信号就用「启动 exe 后 exe 旁是否生成 ops_data.db」。
- **打包三坑**（换环境重打包必看，已写进 README）：
  1. 本机 site-packages 装过的 torch 会被 `openai` 的依赖分析连带拉进打包图，
     收集 CUDA 动态库时**无报错卡死 10+ 分钟**——`--exclude-module torch 系` 后 3 分钟出包；
  2. `--collect-all` 慢一个量级，customtkinter 只需 `collect-data` + `collect-submodules`；
  3. `tasklist` 对中文进程名有编码坑，进程验证改用 `Get-Process` 按 Path 过滤。
- **交付链路闭环**：单文件 exe 58.8MB → 桌面快捷方式换新指向 exe（经 shell 解析 +
  实际拉起进程的端到端验证）→ 空库首页「载入演示数据」一键引导完整演示。

---

## v4.2 · 早报智能体（2026-09-20）

**动机**：对照在招 JD（游戏运营实习生·数据方向）的三条硬要求逐项盘点——
数据体系（v4.1 已覆盖）、AI 应用能力（工具里 AI 只当「顾问」，缺一个真正落地的智能体）、
全球化视野（另附 161 页独立研究）。v4.2 补的就是中间那条：**把「发现异动」推进到「开晨会」**。

**架构：感知 → 决策 → 产出**（`agent.py`，独立模块不耦合 UI）：

1. **感知层**（四类状态探针）：数据新鲜度（最新数据日距今天数）、留存下滑
   （当日 vs 前值越线 -1.5pp）、留存×互动率交叉验证、逾期任务数、预算水位（>90% 告警）。
   阈值全部**复用数据层同一套常量**（`ANOMALY_RULES` / `valid_retention`），不另立口径——
   智能体说「留存崩了」和数据层说「留存崩了」必须是同一句话。
2. **决策层**（确定性规则路由）：留存下滑 → 才去交叉查互动率 → 归因分派
   （互动率同步走低 → 内容吸引力回落；互动率平稳 → 新增渠道质量波动）；
   每条异动映射到具体动作（DAU 异动 → 拆游戏维度看贡献）。
3. **产出层**：headline + 分级预警（danger/warn/info）+ 建议动作 + **决策轨迹 trace**。
   LLM 只负责表达润色（`_ai_polish`，配 Key 才启用，失败自动回退规则模板）——
   **规则管「对不对」，LLM 管「好不好听」**：数据判断交给 LLM 不可复现也不可审计，
   回退路径保证没 Key、断网、超时都不出错报。

**为什么 trace 是一等功能**：运营 AI 不可解释就没法信。每次生成留痕
「感知到了什么 → 为什么查交叉指标 → 查到了什么 → 归因到哪」，
UI 里折叠区直接可见——面试讲「智能体可解释性」有实物可指。

**UI**：总览页右上「生成今日早报」按钮 → 680x740 抽屉（headline / 正文卡 /
分级预警列表 / 建议动作 / 决策轨迹折叠区），头部徽标标注「AI 润色」或「规则生成」。

**演示数据对齐 4.5**：角色池/版本号/事件/风险标题全部更新到
当前真实版本 4.5「挥掷千星的筹码」（知更鸟·晴歌/砂金·戏浪双SP，复刻风堇/不死途，
千星城开放）；drop 触发角色同步换名——**seed 名单被测试断言引用，改名必须连测试一起改**
（flow_test 断言改为姬子·启行/风堇）。初版曾误信过时情报对齐 3.8，
发布前经玩家核实纠正——演示数据的版本语境必须以玩家一手信息为准。

**验收**：测试 70 → **74 项**（新增：智能体全链路结构、确定性造数异常捕获、
纯函数归因分派 ×3 场景、演示库端到端 + 抽屉渲染）；14 张截图（新增 11_早报智能体）。

---

每一版都在回答上一版暴露出的下一个问题：

> 审计发现数据不可信 → **v3.1** 让数据可信、界面像成品 →
> 视觉立住后缺「专业度里子」 → **v4.1** 补数据运营的看家指标 →
> 作品再好，在面试官机器上跑不起来等于零 → **工程交付**闭环 →
> JD 盘点出「AI 工程能力」缺口 → **v4.2** 落地一个可解释的智能体 →
> 智能体有了，但结论可信度、代码可维护性还没兜底 → **v4.3** 做工程加固。

贯穿全程的三条方法论：

1. **先审计 / 先方案 / 先定口径，再动手**——代码审计产出修复清单、
   换装先出三套方案、留存先定三个口径决定；
2. **每一步都有验收兜底**——测试基线（0 → 61 → 70 → 81）+ 截图肉眼验收
   + 对测试本身做故障注入验证；
3. **踩过的坑沉淀成文档**——打包三坑、Tk 能力边界、口径决定全部落进
   README / 本文档，换人换环境可复现。

## v4.3 · 工程加固（2026-09-20）

**动机**：v4.2 之后盘了一遍代码，发现三个「能跑但站不住」的地方——
异动结论没有确认机制、早报给完建议就断、9k 行代码零静态检查。

### ① 异动确认三维度：从「对冲节律」到「消除结构节律」

7 日均值对冲得了周期性，对冲不了节假日与版本活动。新增三个字段：
`wow_change`/`confirmed`（同环比幅度比 ≥0.5 才算真异动）、`streak`（连续同向天数）、
`special`（活动期标记）；分级 danger / warn / watch。

**判据踩坑记**：第一版用「同环比方向同向」判断，写测试时被抓——
单日 DAU 腰斩时上周同日是正常值，方向当然同向，可那是真异动不是节律。
改成**幅度比例**判据才分得开。**判据设计必须靠反例验证，不能拍脑袋**，
这条已写进代码注释留档。

### ② 早报闭环：建议动作一键转待办

任务进 `checklists`、预算类风险进 `risks`，同名条目幂等跳过（重复点不堆积）。
映射规则写在 `agent.py`（运营规则在数据层），UI 只负责触发。

### ③ 质量基建四件套

- **ruff**：渐进策略（先只开 F/E4/E7/E9），清 87 处存量问题，CI 独立 lint job；
- **pytest 适配层**：既有脚本当整体用例跑（中文明细不丢）+ 6 个纯逻辑用例秒级反馈；
- **CI 三层**：lint → fast-test（秒级）→ smoke-test（Windows GUI 全量 81 项），
  让 PR 上先红的永远是最快最便宜的那个；
- **Release 自动化**：推 `v*` tag → 打包 → **exe 冒烟**（校验 exe 旁生成
  `data/ops_data.db`，专防 onefile 路径 bug 复发）→ 自动建 Release。
  首版 workflow 直接用真实 tag 验证跑通，不是纸面配置。

顺带补：**数据源适配层**（`data_source.py`，CSV/API/手动三源共用一套口径，
接入新来源只加一个类）、**结构化日志**（滚动文件落 exe 旁 `logs/`，只落本地不上报）。

**主动放弃**：覆盖率徽章（测试跑在子进程统计不到、GUI 占比大失真 —— 宁缺勿假）。

**三条教训**（均已在 README 留档）：
1. 新增 UI 代码必须跑一遍 GUI 测试 —— v4.3 用了 theme 里不存在的常量名
   （`RADIUS_FULL`/`BG_HOVER`）和漏 import 的 `PRIMARY_HOVER`，被测试当场抓到 NameError；
2. 抽象层要做就做彻底 —— 别名映射迁到 `data_source.py` 后必须删掉
   `views/data.py` 的旧定义，否则 ruff F811 立刻报重复定义；
3. 演示数据的版本语境必须以玩家一手信息为准（v4.2 已被 SEO 过时情报坑过一次）。

---

## 数字盘点

| 维度 | 数字 |
|------|------|
| Python 代码 | ~9,600 行（不含 `_legacy/` 旧版留档） |
| 测试 | 0 → 81 项（渲染冒烟 47 + 业务流程 34）+ 6 项 pytest 纯逻辑；含布局不变量故障注入 |
| 静态检查 | ruff 零告警（v4.3 清理 87 处存量问题），CI 独立 lint job |
| 修复缺陷 | v3.1：数据正确性 8 类 + 界面 7 类 + 其他 5 项；v4.1：真 bug 2 个；v4.3：判据设计错 1 处 + UI 常量错 2 处 |
| 截图验收 | 14 张（遮挡/黑帧自动校验，脏帧跳过而非存错图） |
| 演示数据 | 7 版本 / 105 天指标 / 38 任务 / 7 预算 / 4 风险 / 28 角色使用率 / 10 社区热度（对齐 4.5 版本） |
| 交付物 | 单文件 exe，双击即用（Windows），空库有引导 |
| git | 真实 commit 链，演进可回放 |
