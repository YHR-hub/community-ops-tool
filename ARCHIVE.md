# 归档说明 · 米游社运营助手 v3.1（独立版）

> **这是「社区运营工具」项目的前身版本，归档用于保留完整迭代记录。**
> 归档日期：2026-09-23 ｜ 原日期：v3.1 打包于 2026-06-28

---

## 这是什么

米游社运营助手的 **v3.1 独立版**——工具的第一代完整形态（tkinter 桌面应用）。

## 与现行版本（v4.x）的关系

```
2026-06  v3.1 独立版        ← 本归档（tkinter 单体应用）
            ↓ 迁移
2026-06  v4.0 换装          ← 新仓库 community-ops-tool 开始
2026-09  v4.6（现行）       ← 早期版本见当前仓库历史 (main 分支)
```

v4.x 是对本版本的**架构级重写 + 持续迭代**：
- v3.1 的模块化经验（views/ 分层）被继承并重构为 Mixin 架构
- v4.x 演进出了：早报智能体（v4.2）、异动三维度 + CI 基建（v4.3）、
  事件标注 + 报告导出（v4.4）、行业情报页（v4.5）、文库页（v4.6）

## 保留本归档的意义

1. **迭代记录**：从「0 到 1」的初代形态，是「从 v3.1 到 v4.6」演进叙事的起点
2. **设计史料**：undo_manager、stress_test_ops（27KB 压测脚本）等
   在本版本中的原始设计，现行版本有部分取舍
3. **创作连续性**：证明这个作品被持续维护了近 3 个月、跨两个架构

## 运行方式（如需回溯）

```bash
pip install -r requirements.txt
python main.py
# 或双击「启动运营助手.bat」
```

## 文件说明

- `main.py` / `views/` —— 应用主体（tkinter 分层）
- `db.py` / `seed_demo.py` —— 数据层与演示数据
- `stress_test_ops.py` —— 压力测试脚本（27KB，含完整用例）
- `undo_manager.py` —— 撤销管理器（本版本独有）
- `fetch_data.py` —— 数据抓取脚本
- `MHY_Ops_Assistant_v3.1.spec` —— PyInstaller 打包配置

> 注意：本归档只含源码（不含打包产物与用户数据）。
