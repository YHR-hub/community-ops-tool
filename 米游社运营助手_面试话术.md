# 面试话术 — 米游社运营助手项目介绍

## 2 分钟脱稿版本

---

**（问题引入）**
"米游社运营助手解决的是一个真实痛点：运营多款游戏时，数据分散在不同平台，每周做报告手动粘贴复制要花 2-3 小时，而且容易遗漏风险项。

**（核心设计）**
所以我做了这个本地桌面工具，核心有三点：

第一是**数据闭环**。从手动录入每天 DAU、帖子、评论数据，到自动生成折线图和趋势预测，最后导出 CSV 周报——整个流程从 3 小时压缩到 30 秒。数据全部本地存储在 SQLite，离线可用，数据安全有保障。

第二是**风险预警**。排期预算模块会自动检测预算超支情况，超支部分用红色高亮显示；风险矩阵通过概率和影响双维度评估，帮助运营团队系统化地识别和管理版本风险。

第三是**AI 辅助决策**。接了 DeepSeek/OpenAI 兼容接口，支持多种模型切换。运营人员可以直接对话生成活动方案、内容选题、文案优化建议，后台线程异步调用，超时自动切离线兜底话术，保证功能不挂。

**（技术亮点）**
技术上我做了完整工程化。8 个功能模块采用 Mixin 架构组合，代码解耦清晰。数据库层使用 SQLite WAL 模式提升并发性能，操作日志自动清理保持系统轻量化。

最得意的是内容工厂和趋势雷达这两个模块：内容工厂支持微博/B站/小红书三平台文案生成，有四种风格可选；趋势雷达接入 B站 API 真实爬取热点数据，热度评分可视化，还能生成关键词标签云。

**（未来展望）**
如果给我更多资源，我会接入实时埋点 API 替代手动录入，支持多人协作，并且上线 Web 版方便团队同时使用。"

---

## 关键数据（面试中可以穿插）

| 数据 | 数值 |
|------|------|
| 代码量 | 11 个源文件，约 3300 行 Python |
| 数据库 | SQLite，9 张表 |
| 功能模块 | 8 个（数据看板、AI顾问、版本管理、报告生成、排期预算、内容工厂、趋势雷达、AI设置） |
| 快捷键 | Ctrl+1~8 快速切换模块 |
| AI 场景 | 多场景 Prompt 模板 + 离线兜底 |
| 数据持久化 | 本地 SQLite，离线可用 |
| 周报效率提升 | 从 3 小时 → 30 秒 |
| 测试覆盖 | 34 个测试用例，覆盖数据库层、视图层、性能层 |

---

## 分模块介绍（面试官追问时使用）

### 数据看板模块（Ctrl+1）

"这是最常用的模块。支持双标签页：数据工作台用于录入每日运营数据（DAU、新帖、评论、会话时长、互动率），玩家健康度看板展示玩家分层数据。图表用 Canvas 自绘，零额外依赖。支持多维度筛选和 CSV 导出。"

### AI 顾问模块（Ctrl+2）

"基于大模型的智能运营建议。支持 DeepSeek 和 OpenAI 兼容接口，预设多种 Prompt 模板。关键设计是用 threading 实现异步调用，主线程不会被阻塞。失败时有离线兜底话术，保证功能不挂。"

### 版本管理模块（Ctrl+3）

"游戏版本全生命周期管理。4 个标签页：看板展示版本列表和进度，清单管理上线任务，时间线用甘特图展示版本时间轴，对比功能支持双版本数据对比。"

### 运营报告模块（Ctrl+4）

"运营周报/月报自动生成。从 daily_metrics 聚合数据，按模板生成 Markdown 格式报告，支持 AI 智能润色。历史报告可管理，支持 CSV 导出和剪贴板复制。"

### 排期预算模块（Ctrl+5）

"三合一设计：排期管理时间节点，预算管控成本使用，风险预案识别风险。预算超支会用红色高亮预警，风险矩阵通过概率×影响双维度评估。"

### 内容工厂模块（Ctrl+6）

"社媒文案智能生成。支持微博/B站/小红书三平台，三种文案类型（社媒文案/活动海报/投放素材），四种风格（官方/二次元/悬念/热血）。生成的内容保存在历史记录中。"

### 趋势雷达模块（Ctrl+7）

"社媒热点实时追踪。24 种预设热点分类，接入 B站 API 真实爬取数据，热度评分可视化，关键词标签云展示，AI 趋势分析生成结构化报告。"

### AI 设置模块（Ctrl+8）

"后端配置管理。API Key 配置、模型选择、代理设置、连接测试。配置持久化存储在 config 表中，重启自动加载。"

---

## 技术亮点回答要点

### 为什么用 Mixin 而非拆分子窗口？

"Mixin 模式让所有模块共享侧边栏导航、Toast 通知、快捷键、操作日志、数据库连接。跨模块通信通过 `self._toast()` 等方法直接调用，无需事件总线。新增功能只需新建 Mixin 文件并加入 App 类的继承链。"

### 为什么自绘 Canvas 而非引入 matplotlib？

"体积控制是关键考虑。matplotlib + numpy 会使 EXE 从 85MB 膨胀至 200MB+，但运营场景只需要趋势线和数据点标记，Canvas 原语完全够用。而且 Canvas 是即时绘制，matplotlib 首帧渲染需要 1-2 秒。"

### 为什么用 threading 而非 async/await？

"openai SDK 是同步阻塞调用，用 threading 最简单直接。后台线程执行 API 调用，完成后通过 `self.after(0, callback)` 回到主线程更新 UI。生成期间按钮 disable 显示'生成中'，阻止重复提交。"

### 数据安全问题怎么保障？

"运营数据（DAU、预算金额、风险预案）全部存储在本地 SQLite，不上传云端。SQLite WAL 模式提升并发性能，定期备份机制保护数据不丢失。"
